package Misc;

import java.util.Random;

import Characters.Personaje;

public class GameTest {

    /*public static Personaje inicializaPersonaje(String nombre, int nivel, int pv, int atq, int arm, int res, int vel) {
        Personaje p = new Personaje(nombre, nivel, pv, atq, arm, res, vel);
        return p;
    }

    public static Personaje randomizaPersonaje(String nombre) {
        Personaje player = new Personaje();
        Random random = new Random();

        player.setNivel(1);
        player.setPv(random.nextInt(0, 101));
        player.setAtq(random.nextInt(0, 101));
        player.setArm(random.nextInt(0, 101));

        return player;
    }*/
}
